[project]
name = "decp-processing"
description = "An introductory project to Prefect"
version = "0.1.0"
requires-python = ">= 3.8"
authors = [
    { name = "Colin Maudry", email = "colin+decp@maudry.com" }
]
dependencies = [
    "python-dotenv",
    "pandas",
    "pyarrow",
    "tableschema",
    "frictionless",
    "datapackage-to-datasette",
    "ipykernel",
    "prefect",
]

[project.optional-dependencies]
dev = [
    "pre-commit",
    "pytest-env",
    "pytest",
]

[tool.pytest.ini_options]
pythonpath = [
  "src"
]
testpaths = [
    "tests",
]
env = [
    "DECP_ENRICHIES_VALIDES_URL=data/decp_augmente_valides_2025-03-06T09:54:03.321922.csv"
]    
addopts = "-p no:warnings"
